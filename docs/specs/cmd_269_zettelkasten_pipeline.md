# cmd_269 設計書: Zettelkastenパイプライン自動化

**version**: 1.0
**作成日**: 2026-02-28
**作成者**: 軍師
**ステータス**: 殿レビュー待ち

---

## 1. 概要・目的

Obsidian Vault内に蓄積した5種類のインプットソースをAI（LLM）で分析し、
Permanent Note下書き（`04.PermanentNote/draft_*.md`）を自動生成するスクリプトとlaunchdジョブを実装する。

**パイプライン詰まりの現状**:
- LiteratureNote: 53件 → PermanentNote: 3件（92%が未昇華）
- 手作業でFleetingNote→Permanent化は心理的負荷が高く継続しにくい

**解決アプローチ**:
- 5種類のソースをソース別専用プロンプトでLLM処理
- Permanent Note下書きを自動生成し、殿が確認・編集して確定する
- Zettelkasten原則（1ノート1概念・自分の言葉・リンク候補）を自動遵守

**インプットソース（5種）**:
| No. | ソース | パス | 件数 | 特性 |
|-----|--------|------|------|------|
| 1 | FleetingNote | `01.FleetingNote/` | 15件 | 短い思考の種・一時メモ |
| 2 | Kindle | `02.Kindle/` | 多数 | 原著ハイライト（→殿の言葉に変換必須） |
| 3 | LiteratureNote | `03.LiteratureNote/` | 53件 | 記事・Web資料の引用 |
| 4 | DailyNote | `00.Daily/YYYY/` | 日次 | 日常の出来事・内省 |
| 5 | 週次分析レポート | `10.Meta/Analysis/Weekly/` | 蓄積分 | 既分析済み概念の補完用 |

---

## 2. 対象ファイル構成

```
Vault (iCloud):
  ~/Library/Mobile Documents/iCloud~md~obsidian/Documents/Obsidian_root/
    01.FleetingNote/                          ← 入力: フリーティングノート
    02.Kindle/                                ← 入力: Kindleハイライト
    03.LiteratureNote/                        ← 入力: リテラチャーノート
    00.Daily/YYYY/YYYY-MM-DD.md              ← 入力: デイリーノート
    10.Meta/Analysis/Weekly/Analysis_*.md    ← 入力: 週次分析レポート
    04.PermanentNote/
      draft_YYYYMMDD_NNN.md                  ← 出力: 自動生成下書き
      *.md（手書き3件）                       ← 既存Permanent Note（除外対象）
    20.Profile/00.index.md                   ← 参照: 殿のプロフィール

スクリプト格納先:
  ~/Dev/Obsidian_root/zettelkasten_pipeline.sh

ログ:
  ~/Dev/Obsidian_root/.zettelkasten_pipeline.log

処理済み状態管理:
  （Option C: draft_*.md の source frontmatter で逆引き判定 — 外部ファイル不要）

launchd plist:
  ~/Library/LaunchAgents/com.toichiro.obsidian-zettelkasten.plist
```

---

## 3. インプットソース仕様

### 3.1 FleetingNote（01.FleetingNote/）

```markdown
# 例: なぜアウトプットしようとしないのか.md
インプットは貪るように行うが、その情報をアウトプットしようとしないのはなぜなのか？
アウトプットしようと思う仕掛けが必要なのか？
```

**特性**: frontmatterなし。1〜10文の短い思考メモ。
**処理方針**: 潜在する概念・疑問・気づきを抽出し、1ノート1概念のPermanent Noteに昇華。

### 3.2 Kindleハイライト（02.Kindle/）

```yaml
---
kindle-sync:
  bookId: '37059'
  title: エンジニアリングが好きな私たちのための　エンジニアリングマネジャー入門
  author: サラ・ドラスナー、岩瀬義昌
  asin: B0CYQLXRQ2
  lastAnnotatedDate: '2025-05-09'
  highlightsCount: 20
---
```

**特性**: `kindle-sync` frontmatterを持つ。複数のハイライト（原著の言葉）が箇条書き。
**処理方針**: 原著の言葉をそのまま引用しない。殿自身の解釈・言葉として再表現することが必須。
1冊から複数のPermanent Noteを生成可能（ハイライト内の主要概念ごとに1件）。
**最大生成件数**: 1ファイルあたり最大3件（品質維持のため）。

### 3.3 LiteratureNote（03.LiteratureNote/）

**特性**: Web記事・動画・資料のクリップ。引用・URL・短い感想。frontmatterなし〜任意。
**処理方針**: 記事内の主要概念を殿の言葉で再解釈。記事タイトルではなく概念名でノート作成。

### 3.4 DailyNote（00.Daily/YYYY/YYYY-MM-DD.md）

**特性**: cmd_264が生成するfrontmatter（date/summary/tags）付き。日常の出来事・感情記録。
**処理方針**: 繰り返し登場するパターン・気づき・内省から概念を抽出。感情・行動・思考の洞察をPermanent化。

### 3.5 週次分析レポート（10.Meta/Analysis/Weekly/）

**特性**: cmd_265が生成する構造化レポート（長期要約・スコア評価・メタ認知コメント含む）。
**処理方針**: 既に高度な分析済み。スコアの変化トレンドや繰り返し登場する改善提案から概念を抽出。
「デイリーノートが薄い週」の補完として使用（週次レポートがある週 = デイリーが薄くても概念抽出可能）。

---

## 4. 未処理判定設計（Option C推奨）

### 4.1 選択肢評価

| 選択肢 | 方式 | メリット | デメリット |
|--------|------|---------|-----------|
| A | frontmatterフラグ | ノート単位で状態管理 | Kindleノートは`kindle-sync`プラグインが上書き管理 → フラグが消える可能性 |
| B | 処理済みリストファイル | Vault変更不要 | `~/.zettelkasten_processed.txt`はiCloud非対象 → マルチデバイスで不整合 |
| **C** | **出力ファイルの逆引き** | **ステートレス・Vault無変更** | 全draft_*スキャンのコスト（初期は少数のため許容） |

### 4.2 推奨: Option C（出力ファイルの逆引き）

**根拠**:
1. **Kindleノートの保護**: `kindle-sync`プラグインがfrontmatterを自動管理 → Option Aのフラグ追記はプラグインの次回同期で消える可能性がある
2. **マルチデバイス安全性**: 処理済みリスト（Option B）はiCloudのMobile Documents外に置かれるため、複数Macで実行した場合に不整合が起きる
3. **ステートレス設計**: 処理状態をVault内の出力物（draft_*）から完全に導出できる。状態管理ファイルの破損・消失リスクがない

**判定ロジック**:
```bash
# 処理済みソースのセットを構築
function get_processed_sources() {
  grep -r "^source:" "$VAULT/04.PermanentNote/draft_"*.md 2>/dev/null \
    | grep -oP '\[\[.*?\]\]' \
    | sed 's/\[\[//;s/\]\]//'
}

# ソースが処理済みか判定
function is_processed() {
  local source_path="$1"
  get_processed_sources | grep -qF "$source_path"
}
```

**特殊ケース（DailyNote・週次レポート）**:
- DailyNote: ファイルパス（`00.Daily/2026/2026-01-19`）をsourceに記録 → 日付で逆引き
- 週次レポート: ファイルパス（`10.Meta/Analysis/Weekly/Analysis_2026-01-19_2026-01-25`）をsourceに記録

**既存手書きPermanent Note（3件）の除外**:
- 手書きの3件は`source`フィールドを持たない → 自動的にパイプラインの入力候補に入らない
- `draft_`プレフィックスを出力ファイル命名に使用することで手書きと明確に区別

---

## 5. LLMプロンプト設計

### 5.1 共通前置き（全ソース共通）

```
あなたはZettelkasten法で知識を体系化するアシスタントです。

【殿のプロフィール】
{PROFILE_CONTENT}

【既存PermanentNoteの文体・粒度参考（手書き3件）】
{PERMANENT_NOTE_SAMPLES}

【Zettelkasten原則】
- 1ノート1概念: 1つのノートに1つの概念のみを記述する
- 自分の言葉: 原著の言葉ではなく、殿自身の解釈・言葉で書く
- リンク候補: 他の既存ノートと関連する概念があればwikilink形式で記載する

【出力形式（厳守）】
以下の形式で出力してください。JSONやコードブロックは使わず、そのままMarkdownで出力してください:

---DRAFT_START---
タイトル: （10〜30文字の概念名）
本文:
（概念説明 + 殿の言葉に変換した文、200〜400文字）
リンク候補:
- [[既存ノート名]]
- [[関連概念]]
---DRAFT_END---

複数の概念を抽出できる場合は---DRAFT_START---から---DRAFT_END---を繰り返してください（最大{MAX_PER_SOURCE}件）。
Permanent Noteの候補が見当たらない場合は「NO_CANDIDATE」とだけ出力してください。
```

### 5.2 ソース別プロンプト追加指示

#### FleetingNote向け追加指示
```
【入力: フリーティングノート】
以下の短い思考メモから、永続的な概念・洞察・問いを抽出してください。
断片的な思考でも構いません。潜在する概念を1つ以上見つけてください。

{SOURCE_CONTENT}
```

#### Kindle向け追加指示（最重要: 変換義務）
```
【入力: Kindleハイライト】
書籍「{BOOK_TITLE}」（著者: {AUTHOR}）のハイライトです。

⚠️ 重要: 原著の言葉（ハイライト内のテキスト）をそのままコピーしてはいけません。
必ず「殿（比嘉東一郎）がこの概念をどう理解したか」という視点で、殿自身の言葉として再表現してください。
Kindle location参照（`^ref-XXXXX`）は除去してください。

{SOURCE_CONTENT}
```

#### LiteratureNote向け追加指示
```
【入力: リテラチャーノート】
以下の記事・資料から、殿の思考スタイルと関連する重要概念を抽出してください。
記事タイトルではなく、概念の本質をタイトルとしてください。

{SOURCE_CONTENT}
```

#### DailyNote向け追加指示
```
【入力: デイリーノート】
以下の日常記録から、繰り返し現れるパターン・気づき・内省を概念化してください。
感情・行動・思考の洞察でも可です。

{SOURCE_CONTENT}
```

#### 週次分析レポート向け追加指示
```
【入力: 週次分析レポート】
以下はcmd_265が生成した週次自己分析レポートです。既に高度な分析が済んでいます。
このレポートから、まだPermanent Noteになっていない重要概念を抽出してください。
スコアの傾向・改善提案・メタ認知コメントに注目してください。

{SOURCE_CONTENT}
```

---

## 6. Embedding設計判断（v1: 不採用）

### 評価結果

| 案 | 内容 | 判定 | 理由 |
|----|------|------|------|
| **v1** | **LLMの文脈理解のみ** | **✅ 採用** | シンプル。Permanent Note 3件ではembeddingの効果が薄い |
| v2 | nomic-embedでベクトル類似度 | 🔜 将来検討 | PermanentNoteが20件以上になったら有効 |

### v1採用根拠

1. **現状のPermanentNote数が少ない**: 3件のベクトルDBは探索コストに見合わない
2. **LLMの文脈理解で十分**: gemma3:12bはプロンプト内に既存ノート例を渡せばリンク候補を類推できる
3. **段階的実装**: まず動かして価値を検証してからv2拡張に進む方がリスクが低い

**v2拡張のトリガー**: PermanentNote（手書き + draft確定済み）が20件を超えた時点で再検討。

---

## 7. LLMモデル設定

cmd_264/265と同じ方針を踏襲する。

```bash
# デフォルト設定（スクリプト冒頭・config.sh で上書き可能）
LLM_MODEL="${LLM_MODEL:-gemma3:12b}"
LLM_ENDPOINT="${LLM_ENDPOINT:-http://localhost:11434}"
LLM_NUM_CTX_ZETTELKASTEN="${LLM_NUM_CTX_ZETTELKASTEN:-16384}"
```

**暫定採用**: gemma3:12b（Ollama）
**確定方法**: cmd_268（Gemma品質評価）の結果を踏まえて`config.sh`に最終選定を記録。

### トークン上限試算

| ソース | 入力規模 | 必要num_ctx |
|--------|----------|------------|
| FleetingNote | 本文1〜10文 ≈ 200tokens | 8,192 |
| Kindle | ハイライト20件 ≈ 3,000tokens + プロンプト | **16,384** |
| LiteratureNote | 記事全文 〜 5,000tokens + プロンプト | **16,384** |
| DailyNote | 本文 ≈ 500tokens | 8,192 |
| 週次レポート | 2,000〜3,000tokens + プロンプト | **16,384** |

**結論**: `LLM_NUM_CTX_ZETTELKASTEN=16384` を統一デフォルトとする。

---

## 8. 出力ファイル仕様

### ファイル命名

```
$VAULT/04.PermanentNote/draft_YYYYMMDD_NNN.md
```

NNN: 同日内の連番（001〜999）。同日に複数生成される場合の区別。

### frontmatter

```yaml
---
source: "[[03.LiteratureNote/振り返りの重要性]]"
source_type: literature  # fleeting | kindle | literature | daily | weekly
status: draft
generated_at: "2026-02-28T03:00:00"
---
```

`source_type`フィールド: 後から処理ログ分析・品質チェックに活用。

### 本文構造

```markdown
---
source: "[[02.Kindle/エンジニアリングが好きな私たちのための　エンジニアリングマネジャー入門]]"
source_type: kindle
status: draft
generated_at: "2026-02-28T03:00:00"
---

#リーダーシップ #価値観 #マネジメント

# リーダーシップとは明確さの推進

（概念説明 + 殿の言葉に変換した文、200〜400文字）

## リンク候補
- [[Zettelkasten を続けるコツ(1)]]
```

---

## 9. バッチ実行仕様

### 1回の処理件数制限

| 設定 | 値 | 根拠 |
|------|----|------|
| `MAX_SOURCES_PER_RUN` | 5件 | 殿の確認・編集が追いつくペースに合わせる |
| `MAX_DRAFTS_PER_SOURCE` | 3件（Kindle）/ 1〜2件（他） | 品質維持・処理時間制御 |
| 合計最大draft生成数/回 | 約15件 | |

```bash
MAX_SOURCES_PER_RUN="${MAX_SOURCES_PER_RUN:-5}"
MAX_DRAFTS_PER_KINDLE="${MAX_DRAFTS_PER_KINDLE:-3}"
MAX_DRAFTS_PER_SOURCE="${MAX_DRAFTS_PER_SOURCE:-2}"
```

### 処理優先順位（同日に複数ソースがある場合）

1. FleetingNote（最も軽量・蒸留が急務）
2. LiteratureNote（蓄積量が多い）
3. Kindle（豊富なハイライト）
4. DailyNote（直近のものから）
5. 週次分析レポート（補完目的）

---

## 10. ntfy通知仕様

```bash
# scripts/ntfy.sh を使用（cmd_264/265と同様）

# 成功時
bash ~/Dev/multi-agent-shogun/scripts/ntfy.sh \
  "📝 Zettelkasten: ${DRAFT_COUNT}件の下書きを生成。${DRAFT_TITLES}"

# 燃料不足時（未処理ソースなし or LLMが概念なしと判定）
bash ~/Dev/multi-agent-shogun/scripts/ntfy.sh \
  "⚠️ Zettelkasten: 新規インプットなし。Literature Noteを追加してください"

# エラー時
bash ~/Dev/multi-agent-shogun/scripts/ntfy.sh \
  "❌ Zettelkasten: エラー発生。ログを確認: ~/.zettelkasten_pipeline.log"
```

---

## 11. スクリプト仕様（zettelkasten_pipeline.sh）

### ファイルパス

```bash
SCRIPT="$HOME/Dev/Obsidian_root/zettelkasten_pipeline.sh"
VAULT="$HOME/Library/Mobile Documents/iCloud~md~obsidian/Documents/Obsidian_root"
LOG="$HOME/Dev/Obsidian_root/.zettelkasten_pipeline.log"
CONFIG_FILE="$HOME/Dev/Obsidian_root/config.sh"
[ -f "$CONFIG_FILE" ] && source "$CONFIG_FILE"
```

### 引数・オプション

```bash
# 通常実行（未処理ソースを優先順位順に最大5件処理）
./zettelkasten_pipeline.sh

# ソース種別を絞り込む
./zettelkasten_pipeline.sh --source-type kindle
./zettelkasten_pipeline.sh --source-type literature

# 特定ファイルを指定処理
./zettelkasten_pipeline.sh --file "03.LiteratureNote/振り返りの重要性.md"

# ドライラン（ファイル書き込み不実施・標準出力のみ）
./zettelkasten_pipeline.sh --dry-run

# 再処理（処理済みソースも強制再処理）
./zettelkasten_pipeline.sh --reprocess --file "02.Kindle/エンジニアリングが好きな..."
```

### 処理フロー

```
1. Profile（20.Profile/00.index.md）を読み込む
2. 既存PermanentNote手書き3件のサンプルを読み込む
3. 処理済みソースセットを構築（draft_*.mdのsource frontmatter逆引き）
4. 優先順位に従い未処理ソースをスキャン（最大MAX_SOURCES_PER_RUN件）
5. 各ソースに対してソース別プロンプトを構築
6. Ollamaにリクエスト送信（$LLM_MODEL, num_ctx=$LLM_NUM_CTX_ZETTELKASTEN）
7. レスポンスを---DRAFT_START---/---DRAFT_END---でパース
8. draft_YYYYMMDD_NNN.md をアトミック書き込み（tmpファイル→mv）
9. 生成できた件数をカウント
10. ntfy通知
11. ログ記録
```

### エラーハンドリング

| エラー状況 | 動作 |
|-----------|------|
| Ollama未起動 | ERROR ログ → ntfy通知 → 終了 |
| $LLM_MODEL未pull | ERROR ログ → ntfy通知 → 終了 |
| LLMレスポンスが"NO_CANDIDATE" | ログ記録してスキップ（正常扱い） |
| レスポンスパース失敗 | WARN ログ → そのソースをスキップ → 次のソースへ |
| iCloudファイルロック | リトライ3回 → 失敗でERROR ログ |

### iCloud アトミック書き込み

```bash
TMP_FILE=$(mktemp)
echo "$DRAFT_CONTENT" > "$TMP_FILE"
mv "$TMP_FILE" "$VAULT/04.PermanentNote/draft_${DATE}_${SEQ}.md"
```

---

## 12. launchd設定

### 実行スケジュール（既存ジョブとの競合回避）

| ジョブ | plist名 | 実行タイミング |
|--------|---------|--------------|
| cmd_264 daily-ai | com.toichiro.obsidian-daily-ai | 毎日 0:15 |
| cmd_265 weekly | com.toichiro.obsidian-weekly-analysis | 毎週月曜 0:30 |
| cmd_265 monthly | com.toichiro.obsidian-monthly-analysis | 毎月1日 1:00 |
| cmd_265 yearly | com.toichiro.obsidian-yearly-analysis | 1月1日 2:00 |
| **cmd_269 zettelkasten** | **com.toichiro.obsidian-zettelkasten** | **毎週日曜 3:00** |

**日曜3:00を選んだ理由**: 週の終わりに1週間のインプットを整理する自然なタイミング。既存の月曜0:30（週次分析）の前夜に当たらないため競合しない。

### plist設定

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.toichiro.obsidian-zettelkasten</string>

  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>/Users/toichiro/Dev/Obsidian_root/zettelkasten_pipeline.sh</string>
  </array>

  <key>StartCalendarInterval</key>
  <dict>
    <key>Weekday</key>
    <integer>0</integer>
    <key>Hour</key>
    <integer>3</integer>
    <key>Minute</key>
    <integer>0</integer>
  </dict>

  <key>StandardOutPath</key>
  <string>/Users/toichiro/Dev/Obsidian_root/.zettelkasten_pipeline.log</string>

  <key>StandardErrorPath</key>
  <string>/Users/toichiro/Dev/Obsidian_root/.zettelkasten_pipeline.log</string>

  <key>RunAtLoad</key>
  <false/>
</dict>
</plist>
```

---

## 13. テスト計画

### 単体テスト（スクリプト直接実行）

| テストケース | 条件 | 期待動作 |
|------------|------|---------|
| T1: FleetingNote処理 | 未処理FleetingNoteあり | draft_*.md生成・source frontmatterにFleetingNoteパス記録 |
| T2: Kindle処理 | 未処理Kindleノートあり | 原著引用なし・殿の言葉で再表現されたdraft生成 |
| T3: 処理済み判定 | 同じソースを2回実行 | 2回目はスキップ（is_processed判定） |
| T4: 全件処理済み | 未処理ソースなし | 「NO_CANDIDATE」ログ → ntfy「燃料不足」通知 |
| T5: Ollama未起動 | ollamaサービス停止 | ERROR ログ → ntfy通知 → 正常終了 |
| T6: ドライラン | --dry-run | 標準出力のみ・ファイル変更なし |
| T7: 特定ファイル指定 | --file オプション | 指定ファイルのみ処理・MAX_SOURCES制限無視 |
| T8: 手書きPermanentNote除外 | 実行 | 既存3件がソース候補に入らない |

### 統合テスト（手動実行確認）

```bash
# STEP 1: ドライランで候補ソース確認
./zettelkasten_pipeline.sh --dry-run

# STEP 2: FleetingNoteのみ実行
./zettelkasten_pipeline.sh --source-type fleeting

# STEP 3: 生成ファイル確認
ls -la ~/Library/Mobile\ Documents/iCloud~md~obsidian/Documents/Obsidian_root/04.PermanentNote/draft_*.md

# STEP 4: Kindleノートで変換品質確認（Kindle 1冊）
./zettelkasten_pipeline.sh --source-type kindle --max-sources 1

# STEP 5: 同じファイルを再実行してスキップ確認
./zettelkasten_pipeline.sh --source-type fleeting  # → 全てスキップされること
```

---

## 14. 設計上の考慮点と決定事項

| 項目 | 決定内容 | 根拠 |
|------|---------|------|
| 未処理判定方式 | **Option C（逆引き）** | Kindle同期プラグイン保護・iCloud外のリスト不要 |
| Embedding使用 | **v1: 不採用** | PermanentNote 3件では効果薄。20件超で再検討 |
| 1回実行制限 | **最大5ソース** | 殿の確認ペース（週1回）に合わせる |
| スケジュール | **日曜 3:00** | 既存ジョブとの競合なし。週末整理の自然なタイミング |
| LLMモデル | **gemma3:12b（暫定）** | cmd_268評価後にconfig.shで確定 |
| Kindle変換 | **原著引用禁止・変換義務** | Zettelkasten「自分の言葉」原則の最重要ポイント |
